const ProofAction = ({ commitmentId }) => {
  return (
    <div style={{ marginTop: 16 }}>
      <button>
        Submit today’s proof
      </button>
    </div>
  );
};

export default ProofAction;
