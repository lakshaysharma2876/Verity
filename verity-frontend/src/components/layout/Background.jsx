import Aurora from "./Aurora"
const Background = ({ children }) => {
    
  return (
    <div style = {{
    "background-color":'white',
  }}
>
      {children}
      </div>
  );
};

export default Background;
